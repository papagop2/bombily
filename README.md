# Бомбилы

Мобильное приложение для заказа такси, грузового такси и доставки с встроенным магазином.

## Описание

Бомбилы - это комплексное приложение, объединяющее в себе:
- **Такси** - заказ пассажирских перевозок
- **Грузовое такси** - перевозка грузов
- **Доставка** - доставка товаров из встроенного магазина

Приложение поддерживает три роли пользователей:
- **Пассажир** - создание заказов
- **Водитель** - принятие и выполнение заказов
- **Администратор** - управление системой

## Особенности

- 🎨 Серый фон с контрастным дизайном (белый, черный, желтый)
- 📱 Полностью адаптивный дизайн для мобильных устройств
- ⏰ Функция "вызов по времени" для всех типов заказов
- 🌆 Система городов с предложениями от пользователей и модерацией в админке
- 🏬 Магазины в каждом городе (магазин → подкатегории → товары) с общей наценкой
- 🚚 Отдельный тариф «доставка и сборка» на каждый город
- 🚦 Расширенные статусы заказов для водителей и пассажиров с подсказками по действиям
- 🛒 Встроенный магазин с каталогом товаров
- 🔊 Аудио уведомления о новых заказах
- 🔐 Аутентификация через Supabase
- 📊 Административная панель для управления

## Технологии

- **Next.js 14** - React фреймворк (режим server + API роуты)
- **TypeScript** - строгая типизация
- **Supabase Auth + RLS** - база данных, безопасная аутентификация и правила доступа
- **Tailwind CSS** - стилизация
- **Framer Motion** - анимации
- **Zustand** - управление состоянием
- **date-fns** - работа с датами
- **Capacitor** - для сборки APK

## Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта (см. `env.example`):

```env
NEXT_PUBLIC_SUPABASE_URL=https://zlgayvflgfczmfvlycxo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpsZ2F5dmZsZ2Zjem1mdmx5Y3hvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM5MDcyNTYsImV4cCI6MjA3OTQ4MzI1Nn0.eP9hyP5-pHiKtp3qXGIurRX14xBiQAI2fNiC6A2e3Pw
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
NEXT_PUBLIC_APP_URL=https://your-production-domain.example.com
```

**Важно:** `SUPABASE_SERVICE_ROLE_KEY` используется только на сервере (Next API роуты) и никогда не должен попадать в git. Создайте `.env.local`, добавьте ключ и держите файл только локально.

### 4. Настройка базы данных Supabase

1. Откройте панель Supabase: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в SQL Editor
4. Выполните SQL скрипт из файла `supabase/schema.sql`

Это создаст все необходимые таблицы:
- `cities` — справочник городов
- `city_proposals` — предложения городов от пользователей
- `users` — пользователи системы (с расширенными полями для водителей)
- `categories` — категории товаров
- `products` — товары для доставки
- `orders` — заказы (с расширенными статусами)
- `delivery_items` — элементы заказов доставки

### 5. Настройка аутентификации в Supabase

1. В панели Supabase перейдите в Authentication → Settings
2. Убедитесь, что Email provider включен и разрешено создание пользователей без подтверждения (либо настройте SMTP)
3. Приложение использует номера телефонов как логины и автоматически преобразует их в техническую почту вида `79991234567@bombily.local`
4. Первый зарегистрированный пользователь автоматически получает роль администратора

**Важно:** Настройте требования к паролю в Supabase и при необходимости включите подтверждение email, чтобы предотвратить спам-регистрации.

### 6. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## Использование

### Первый запуск

1. При первом запуске откроется модальное окно с запросом разрешения на аудио уведомления
2. **Первый зарегистрированный пользователь автоматически получит роль "Администратор"**

### Функции

- **Пассажир:** Выбор подтверждённого города или предложение нового, создание заказов всех типов, отслеживание статусов («принят», «водитель в пути», «ожидание», кнопка «🚶 Выхожу» и т.д.)
- **Водитель:** Регистрация с указанием автомобиля и данных СБП, фильтрация заказов по городу, последовательность действий («Принять» → «🚀 Выехал» → «🎯 Прибыл» → «✅ Завершить») с подсказками для оплаты
- **Администратор:** Управление заказами/пользователями/товарами/категориями/магазинами, модуль городов (добавление/редактирование/тарифы), модерация заявок на города и назначение городов пользователям, настройка глобальной наценки

### Города и предложения

- Пользователь при регистрации выбирает город из списка или предлагает новый
- Предложенный город отображается администратору в отдельной вкладке, он может отредактировать название и добавить город одним кликом
- После добавления города администратор может сразу привязать его к пользователю
- Для каждого города администратор задаёт фиксированную сумму «доставка и сборка», которая автоматически прибавляется к корзине

### Статусы заказа

Для водителя:
1. **Принят** — кнопка «🚀 Выехал»
2. **В пути** — кнопка «🎯 Прибыл»
3. **Прибыл** — ожидание пассажира, кнопка «✅ Завершить»
4. **Пассажир выходит** — после кнопки «🚶 Выхожу» у пассажира
5. **Завершен** — карточка с реквизитами СБП

Для пассажира:
1. Уведомление о принятии заказа и данных водителя
2. Уведомление о выезде/прибытии
3. Кнопка «🚶 Выхожу» при прибытии
4. Финальное сообщение с просьбой оплатить по СБП

### Магазины и доставка

- Магазины привязаны к конкретным городам. Внутри каждого магазина администратор создаёт подкатегории и товары
- Администратор задаёт единую наценку (%) на все товары. Пользователь видит цены уже с наценкой, округлённой в большую сторону
- В одном заказе можно собирать товары только из одного магазина. Корзина автоматически блокирует добавление товаров из разных магазинов
- Пользователь видит каталог своего города, может искать по всем магазинам сразу и внутри выбранного магазина
- При оформлении заказа панель показывает: товары, наценку, стоимость «доставка и сборка» по городу и итоговую сумму

### Вызов по времени

Для всех типов заказов:
- Сегодня: не ранее чем через 2 часа
- Завтра: любое время
- Выбор дня кнопками, времени - отдельными полями

## Сборка APK

**См. подробную инструкцию:** [BUILD_APK.md](BUILD_APK.md)

Кратко:
1. `npm install`
2. `npm run build`
3. `npx cap add android`
4. `npx cap sync android` (Capacitor будет открывать URL из `NEXT_PUBLIC_APP_URL`, поэтому перед сборкой задеплойте Next.js на HTTPS-домен)
5. Откройте в Android Studio и соберите APK

---

**Бомбилы** - Удобное решение для такси, грузоперевозок и доставки! 🚕📦

## Безопасность и роли

- Вся аутентификация перенесена в Supabase Auth. Пароли хэшируются Supabase, а сессии проверяются через JWT.
- Серверный маршрут `/api/admin/bootstrap` выдаёт административные данные только при наличии валидного токена и роли `admin`.
- Row Level Security теперь строго проверяет роли: пассажир видит только свои заказы, водитель — заказы своего города, администратор — весь массив данных.
- Сервисный ключ Supabase хранится только на сервере и используется для проверок внутри API. Никогда не коммитьте его в репозиторий.

## Тестирование

```bash
npm test
```

Vitest + Testing Library покрывают базовые пользовательские сценарии (валидация заказов, нормализация телефонов). Новые тесты размещайте в папке `__tests__`.
